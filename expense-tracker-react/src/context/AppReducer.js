// Reducer is basicall how we specify application state changes in response to certain actions
export default (state, action) => {
    switch(action.type) {
        default:
            return state;
    }
}